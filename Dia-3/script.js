let numUm = 5;
let numDois = 3;
let soma = numUm + numDois;
let sub = numUm - numDois;
let multi = numUm * numDois;
let divi = numUm / numDois;
let mod = numUm % numDois;

console.log("A soma dos numeros:", numUm, "e", numDois, "é igual = ", soma);
console.log("A subtração dos numeros:", numUm, "e", numDois, "é igual = ", sub);
console.log(
  "A multiplicação dos numeros:",
  numUm,
  "e",
  numDois,
  "é igual = ",
  multi
);
console.log("A divisão dos numeros:", numUm, "e", numDois, "é igual = ", divi);
console.log(
  "O resto da divisão dos numeros:",
  numUm,
  "e",
  numDois,
  "é igual = ",
  mod
);
